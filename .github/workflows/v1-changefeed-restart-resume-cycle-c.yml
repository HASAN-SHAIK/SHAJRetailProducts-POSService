name: V1 Changefeed Restart Resume Cycle C

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout exact pull request head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Assert exact head
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
          echo "EXACT_HEAD=$(git rev-parse HEAD)"

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
          cache: true

      - name: Focused existing reconnect acceptance
        run: go test ./internal/changefeed -run '^TestV1ReconnectConvergesConfigAndChangeFeedAfterSQLiteRestart$' -count=1 -v

      - name: Full Go suite
        run: go test ./...

      - name: Vet
        run: go vet ./...

      - name: Build production POSService
        run: go build -o /tmp/posservice-cycle-c ./cmd/posservice

      - name: Check runtime harness
        run: |
          bash -n tests/v1-changefeed-restart-runtime.sh
          python3 -m py_compile tests/changefeed-restart-central.py

      - name: Real POSService restart and changefeed resume runtime
        run: bash tests/v1-changefeed-restart-runtime.sh /tmp/posservice-cycle-c
