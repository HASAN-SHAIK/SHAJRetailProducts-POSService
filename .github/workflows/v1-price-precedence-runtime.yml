name: V1 Price Precedence Runtime

on:
  pull_request:
    paths:
      - 'internal/server/price_precedence_runtime_test.go'
      - '.github/workflows/v1-price-precedence-runtime.yml'
  workflow_dispatch:

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Focused live HTTP plus SQLite price precedence verification
        run: go test ./internal/server -run '^TestEffectivePricePrecedenceOverLiveHTTPServer$' -count=1 -v
      - name: Full POSService suite
        run: go test ./...
      - name: Vet
        run: go vet ./...
      - name: Build runnable
        run: go build ./cmd/posservice
