# SHAJRetailProducts V1 Security Acceptance Matrix

Status: **IN PROGRESS**

## Authority boundary

- Central owns tenant identity, roles/permissions, POS registration, branch/store trust, offline-grant signing, canonical recovery authorization and tenant PostgreSQL authority.
- POSService is a small offline edge runtime. It verifies Central-issued grants, persists only local derived/session state required for offline operation, and must never become a tenant/permission/key authority.
- Frontend is presentation/orchestration only. Central browser credentials remain HttpOnly-cookie based and local POS access remains bound to the packaged POS local session/machine boundary.
- Security work must reuse already-certified Auth/Az, Store/Device, Inventory, Catalog, Database and Observability boundaries rather than introduce parallel security models.

## Acceptance matrix

| Capability | Current evidence | Status | Release requirement |
|---|---|---|---|
| Tenant database isolation | Customers/Reporting/Auth certifications prove supplied tenant pools remain isolated and cross-tenant token/grant use fails closed | CERTIFIED | retain cross-tenant PostgreSQL/session/grant acceptance |
| Store/branch isolation | Store/Device, Inventory, Catalog and Reporting certifications bind restricted users/devices to trusted Central branches | CERTIFIED | preserve trusted branch authority on reads/writes/sync |
| POS device identity and revocation | Store/Device V1 certifies first-run registration, active-device sync authority, explicit reassignment, revocation and replacement | CERTIFIED | preserve active Central registration requirement for new machine facts |
| Central tenant browser session | Auth/Az V1 certifies HttpOnly cookie-only access tokens, refresh rotation/replay protection and 401/403 handling | CERTIFIED | preserve secure cookie flags and token replay protection |
| Platform-admin key isolation | Auth/Az V1 certifies platform-admin signing/verification does not fall back to tenant JWT secret | CERTIFIED | preserve separate key boundary |
| Offline grant signing and scope | Auth/Az V1 certifies Central current-user/device/branch authority, RS256 verification, tenant/device/store binding and expiry | CERTIFIED | preserve current-authority reload before grant issuance |
| POS local PIN/session security | Auth/Az V1 certifies PBKDF2-derived PIN storage, lockout, hashed local sessions, logout and re-enrollment invalidation | CERTIFIED | preserve no plaintext PIN/session-token persistence |
| POS local machine/origin boundary | Auth/Az V1 certifies protected loopback routes require machine token and approved browser origin | CERTIFIED | preserve public health/readiness exceptions only |
| Request/error/diagnostic secret hygiene | Observability V1 certifies stable 5xx responses, safe bounded request IDs and credential-safe diagnostics; Backend #124 applies the same stable `INTERNAL_ERROR / Internal Server Error` boundary to product-import controllers that handle their own exceptions; Backend #127 redacts production startup failure detail to stable `INTERNAL_STARTUP_ERROR` | CERTIFIED | preserve no secret/query/payload/internal-path leakage in support, startup, or controller-local 5xx surfaces |
| Migration/backup tenant protection | Database Quality V1 certifies checksum drift rejection, same-tenant native restore and cross-tenant restore protection | CERTIFIED | preserve verified manifest/checksum/tenant binding |
| Secret defaults/fail-closed startup | Auth/Az requires Central tenant/platform JWT secrets; POS #289 requires the offline-grant public key in production and rejects placeholder sync/local secrets; Backend #108 requires an explicit production RabbitMQ URL when messaging is enabled and rejects embedded `guest:guest` credentials; Backend #113 centralizes PostgreSQL TLS policy for master/admin/tenant pools, verifies server certificates by default whenever `DB_SSL=true`, supports an explicit CA, and rejects production `DB_SSL_REJECT_UNAUTHORIZED=false`; Backend #114 requires an explicit valid `SUPPORT_CASE_INTAKE_EMAIL` in production; Backend #115 runs the production container with `NODE_ENV=production` as the non-root Node user; Backend #117 validates privileged platform-admin seed configuration; Backend #118 removes hard-coded master-bootstrap administrator credentials; Backend #120 requires header-only timing-safe health warmup authentication | PARTIAL | finish repository-wide production secret/config audit; no production fallback/default secrets, unintended destinations, disabled infrastructure certificate verification, weak privileged seed/bootstrap configuration, URL-secret transport, or root container runtime |
| Cookie/CORS/CSRF boundary | Auth/Az certifies HttpOnly `Secure`/`SameSite=Lax` browser cookies; Backend #106 rejects production credentialed wildcard CORS and permits only explicitly configured browser origins while retaining no-Origin machine clients | CERTIFIED | preserve explicit production origin allowlist and secure cookie flags |
| Central login/brute-force protection | Backend #105 shares bounded login/refresh limiters across legacy and `/api/v1` tenant auth aliases; failed logins are capped without consuming allowance on successful logins; platform-admin login was already bounded | CERTIFIED | retain auth-sensitive rate limits without blocking health/sync operation |
| Dependency/supply-chain review | POS Go dependencies are checksum-pinned; Frontend #78/#79 use committed lockfile + reproducible production build dependencies; Backend #110 commits `package-lock.json` and validates `npm ci`/`npm ls`; Backend #119 removes verified-unused direct dependencies; Backend #121 keeps production Puppeteer sandboxing mandatory; Backend #126 explicitly records the remaining runtime-reachable `nodemailer`, `puppeteer`, and `xlsx` advisories as **accepted with mitigations for V1, not fixed**, and adds executable acceptance preventing removed dependencies or required mitigations from silently regressing | CERTIFIED | keep advisory reporting active; any widened runtime reachability, removed mitigation, new direct high/critical dependency, or future compatible upgrade requires Security review |
| Upload/import/file path boundary | Central JSON bodies are capped at 5 MB; Backend #123 enforces `/products/import-rows` max 500 rows; Backend #109 keeps purchase-invoice uploads memory-only/5 MB with MIME + extension consistency; Backend #112 verifies PDF/PNG/JPEG signatures; Backend #122 verifies XLSX/XLS/PDF/CSV import content before parser work; Backend #128 prevents direct request-controlled filesystem/process paths, rejects disk-backed multer/`req.file.path`, and keeps operator-only same-tenant PostgreSQL restore paths outside browser/POS HTTP authority | CERTIFIED | preserve memory-only bounded HTTP uploads, content verification, and separation of operator recovery paths from tenant/browser/POS request authority |
| SQL/query injection boundary | Primary services use parameterized PostgreSQL queries in certified paths; the data-quality backup's dynamic table SQL interpolates only an internal fixed table allowlist; Backend #111 establishes an exact-head inventory of 79 legacy template-interpolated query sites and fails immediately on direct HTTP request-object interpolation; Backend #125 emits the exact structural expressions for classification without exposing runtime values | PARTIAL | finish disposition of the 79 structural-interpolation sites; no caller-controlled SQL identifiers/fragments without strict allowlists |
| Security-sensitive logging | Auth/Observability removed password-hash/access-token/query leakage in certified paths; Backend #116 preserves production DB duration observability while suppressing raw SQL text/literals; Backend #124 keeps product-import parser/database internals out of client-visible 5xx responses; Backend #127 keeps production startup failures secret-safe | CERTIFIED | preserve credential-safe structured logging and never emit raw production SQL text/payload/internal failure details |
| Recovery/approval authority separation | Inventory/Store/Device/Auth/Database domains keep Central-approved recovery separate from read-only support views | CERTIFIED | do not re-expand support or manager-approval authority |
| Frontend secret persistence | Frontend Completion/Auth V1 certifies no Central JWT in JS-readable browser persistence | CERTIFIED | preserve metadata-only browser session persistence |
| Security CI/release gate | POS #302 adds the consolidated merged-main Security release workflow covering POS tests/vet/build, merged Backend Security acceptance with committed lockfile, and merged Frontend Security acceptance/build | CERTIFIED | rerun the merged-main gate after the final remaining Security rows are closed before final release certification |

## Current audit priorities

1. Finish the 79-site structural SQL disposition using Backend #111/#125 evidence; reduce or explicitly allowlist structural fragments so no caller-controlled identifier/fragment can reach SQL.
2. Finish the repository-wide production secret/config audit across all three repositories, preserving the already-merged PostgreSQL TLS, support intake, non-root container, admin seed/bootstrap, header-only warmup, CORS/cookie and secret-safe logging boundaries.
3. Rerun the merged-main three-repository Security release gate after those two remaining rows are closed and record the final release decision.

## Release decision

**NOT YET RELEASE CERTIFIED.** Core tenant/store/device/token/session/recovery security is strongly covered. Dependency advisories that remain runtime-reachable are explicitly accepted-with-mitigations for V1 rather than falsely marked fixed, and the HTTP upload/file/path boundary is now executable and fail-closed. Final Security release remains blocked on the 79-site structural-query disposition and the remaining repository-wide production secret/config audit; once those are closed, the consolidated three-repository Security gate must be rerun before release.
