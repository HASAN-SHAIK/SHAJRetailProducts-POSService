# SHAJRetailProducts V1 Reporting / Admin Acceptance Matrix

Status: **RELEASE CERTIFIED**

## Authority boundary

- Central Backend/PostgreSQL is canonical for business reporting facts, administrative reporting permissions, tenant isolation, historical sales/returns, inventory value, profit and operational support views.
- POSService/SQLite may expose local operational diagnostics needed during offline operation, but is not the canonical business-reporting engine.
- Frontend is presentation/orchestration only. It must not recompute canonical revenue, profit, tax, inventory value or historical business truth from browser caches.
- Certified Transaction, Inventory, Products/Catalog, Pricing/Tax, Customers, Store/Device and Auth/Az facts are inputs to reporting; Reporting V1 consumes those authorities rather than creating parallel truth.

## Acceptance matrix

| Capability | Current evidence | Status | Release requirement |
|---|---|---|---|
| Central report permission boundary | Backend #76 aligns all five core report routes with `reports:read`; Backend #84 extends the same permission boundary to mobile reporting; Backend #85 applies `reports:read` plus trusted report scope to `/dashboard/overview`; cashier remains denied by the certified role catalog | CERTIFIED | preserve stable server-side permission denial and trusted Central report scope |
| Sales/revenue report | canonical report controllers read tenant PostgreSQL orders, subtract `returned_amount`, include completed/partial/full-return lifecycle and returned item quantities; Backend #78 certifies refund-aware aggregate structure; Backend #80 adds real branch-A/branch-B PostgreSQL isolation; Backend #83 proves full-return net-zero behavior and replay-stable canonical facts; Backend #91 keeps historical product identity/price on immutable order-item snapshots | CERTIFIED | preserve immutable order/return/product snapshots and replay-safe net revenue |
| Daily sales report | Backend #82 derives daily revenue from canonical `orders.total_price - returned_amount`, uses strict UTC `[start,end)` day boundaries, validates `YYYY-MM-DD`, preserves trusted branch filtering and returned-quantity-aware product/profit facts | CERTIFIED | preserve canonical immutable order/return facts, UTC date semantics and trusted branch scope |
| Profit report | Backend #78 certifies returned-quantity-aware profit structure; Backend #80 scopes profit to the trusted branch; Backend #83 proves full-return profit contribution nets to zero and remains replay-stable in PostgreSQL | CERTIFIED | preserve immutable purchase-price/profit snapshots and return-aware replay behavior |
| Inventory report | Backend #87 adds trusted branch inventory reporting from canonical branch facts instead of tenant product totals: physical stock, non-expired sellable stock, expired stock, outstanding provisional/unallocated POS oversell deficit, projected net stock, low/out-of-stock counts and sellable inventory value; real PostgreSQL branch-A/branch-B acceptance proves branch isolation and deficit restoration semantics | CERTIFIED | preserve explicit physical/sellable/expired/provisional-deficit semantics and never collapse provisional offline deficit into fabricated batch stock |
| Dashboard/admin KPI views | Backend #85 requires `reports:read`, reuses trusted Central report scope for `/dashboard/overview`, replaces caller branch input with the resolved branch, and suppresses uncertified branch low/dead-stock widgets while retaining order-derived KPI facts; Backend #84 scopes mobile dashboard sales/profit/recent-order facts; Frontend #73 makes mobile Reports truthful/retryable; Frontend #76 surfaces Central Dashboard reachability failure explicitly and provides `Retry dashboard` without introducing browser report authority | CERTIFIED | preserve trusted Central scope, truthful unavailable/empty states and actionable retry without browser-side canonical reporting |
| Product/category performance | Backend #78/#80/#83 certify returned-quantity-aware trusted-branch product/category aggregates; Backend #91 routes product history through immutable `order_items` product identity/name/price snapshots; Backend #92 persists immutable sale-time category ID/name snapshots and reports older unsnapshotted history as `Unattributed`; POSService #260 snapshots only the Central-projected local category at sale creation and proves the snapshot survives later catalog recategorization, restart and replay across the full order/payment/receipt/refund/partial-return regression suite | CERTIFIED | preserve immutable sale-time product/category snapshots; never rewrite historical attribution from the current catalog and never guess older unsnapshotted category history |
| Tax/GST reporting | Backend #90 replaces the mutable legacy GST-ledger report source with immutable canonical sale/return tax snapshots, trusted branch scope, bounded UTC ranges and cumulative partial/full-return tax credits; real PostgreSQL acceptance proves exact net-zero after full reversal and no recalculation from current tax policy; Frontend #75 presents canonical taxable/GST totals without fabricating jurisdiction splits | CERTIFIED | preserve immutable tax snapshots, cumulative return allocation and no fabricated CGST/SGST/IGST split without jurisdiction evidence |
| Customer/outstanding admin reporting | Backend #89 adds bounded `/reports/customers-outstanding` under `reports:read`, reading only tenant PostgreSQL `customers.current_balance` and `credit_limit` from the already-certified Customers projection; focused acceptance proves the report does not derive debt from POS/browser snapshots or duplicate order/payment/return arithmetic | CERTIFIED | preserve Central customer balance projection as the only reporting authority and bounded tenant-global result semantics |
| Store/branch reporting scope | Backend #77 binds every new canonical POS sale to the trusted active device branch; POS #244 proves restarted SQLite outbox → Central/PostgreSQL branch provenance and replay; Backend #80 applies trusted branch scope to core reports; Backend #83 preserves return/replay correctness; Backend #84/#85 extend trusted scope to mobile/dashboard facts; Backend #87 serves branch inventory only from certified branch inventory truth; Backend #90/#91/#92 preserve trusted scope and immutable tax/product/category history | CERTIFIED | preserve trusted branch provenance; historical null POS branch provenance must remain explicit rather than guessed |
| Tenant isolation | Backend #81 proves identical report requests remain bound to separate supplied tenant PostgreSQL pools; Backend #84 removes the mobile reporting fallback to the default/global pool | CERTIFIED | preserve tenant-pool-only reporting authority across every reporting/admin query |
| Date/range validation | Backend #82 certifies strict UTC daily `YYYY-MM-DD` validation; Frontend #73 removes fake custom ranges from the fixed-window mobile report screen; Backend #86 adds one bounded V1 contract for sales/profit explicit ranges: paired real `YYYY-MM-DD` values, UTC day normalization, reversed/invalid rejection and a 366-day maximum while preserving the existing previous-month default when no range is supplied; Backend #90 applies bounded UTC ranges to canonical GST | CERTIFIED | preserve bounded deterministic UTC range semantics and fail closed before PostgreSQL on malformed or oversized explicit ranges |
| Large-result safety | Backend #86 bounds explicit sales/profit date windows to 366 days; Backend #88 clamps stock-audit detail to 1–500 and customer/product duplicate suggestions to 1–100; Backend #89 bounds customer outstanding results to at most 200 rows per page; Backend #93 completes the admin support/data-quality route inventory under tenant-pool authority, while full backup sizing/streaming is intentionally deferred to the later Backup/Recovery domain | CERTIFIED | preserve bounded supported reporting/support reads; do not treat full tenant backup as a reporting result surface |
| Support/data-quality reporting | Backend #88 certifies bounded stock-audit and duplicate-suggestion result sizes; Backend #93 completes the existing support/data-quality route authority audit across stock audit, manual consistency, duplicate suggestions/merge, backup export and backup verification, preserving admin-only access, tenant-pool isolation and authenticated actor provenance | CERTIFIED | preserve admin-only tenant-scoped diagnostics/mutations and credential-safe operational output |
| Frontend reporting/admin UX | Frontend #73 certifies truthful/retryable mobile reports; Frontend #74 keeps the routed GST screen on Central authority with loading/error/empty/retry states; Frontend #75 presents canonical GST totals without fabricated jurisdiction component values; Frontend #76 makes routed Dashboard Central-unavailability explicit with accessible retry while preserving trusted Backend reporting authority | CERTIFIED | preserve role visibility, loading/error/empty/retry semantics, accessibility and no stale browser reporting authority |
| Export/download behavior | core V1 `/reports` routes expose canonical JSON reads only; no Reporting CSV/PDF/download contract exists. `/data-quality/backup/export` is an admin tenant backup and belongs to the later PostgreSQL/backup/recovery-quality domain, not Reporting V1 | N/A | do not invent reporting export formats; certify backup/export operational behavior in the dedicated Backup/Recovery domain |
| Cross-repository release acceptance | POSService #262 establishes the merged-main Reporting/Admin release gate across POS packages/vet/build, Backend Reporting/Admin acceptance and Frontend reporting acceptance/build; this release-certification change additionally includes the merged Dashboard network-state acceptance and reruns the established PostgreSQL reporting branch E2E through the matrix-triggered validation | CERTIFIED | keep the merged-main release gate green before Reporting/Admin release decisions |

## Current certified evidence

- Backend #76: report permission authority uses the existing `reports:read` catalog and originally failed closed for branch-restricted users until trusted branch query scoping was implemented.
- Backend #77: new canonical POS `sale.completed` orders are transactionally bound to the active Central-registered device branch; missing provenance and cross-branch rebinding fail closed.
- POSService #244: real restarted POS SQLite durable outbox → production Central sync route on merged Backend `main` → PostgreSQL proves canonical POS order branch provenance and duplicate/replay safety.
- Backend #78: existing canonical sales/profit/product aggregates are explicitly certified refund-aware using `orders.returned_amount` and returned item quantities.
- Backend #80: sales, daily, profit, profit graph and product-performance reports use trusted Central branch scope; branch users cannot widen scope with caller input; a real PostgreSQL branch-A/branch-B gate proves isolation plus partial-return net revenue.
- Backend #81: report controllers remain bound to the supplied tenant PostgreSQL pool for identical requests and never fall back to the default/global database authority.
- Backend #82: `/reports/daily` consumes canonical immutable order/return snapshots for revenue, uses strict UTC day boundaries and trusted branch scope, and rejects invalid date-only input before querying PostgreSQL.
- Backend #83: real PostgreSQL branch acceptance includes a fully returned sale, proves that sale contributes zero net revenue/quantity/profit, and verifies replaying the same canonical full-return state does not change reporting output.
- Backend #84: mobile reporting requires `reports:read`, uses certified report scope, scopes mobile sales/profit/recent-order facts to the trusted branch and never falls back to a global DB pool.
- Frontend #73: mobile report UX labels only the fixed Today/Week/Month windows it actually receives, exposes unavailable data as unavailable rather than zero, and provides explicit retry state with production-build acceptance.
- Backend #85: `/dashboard/overview` requires `reports:read`, consumes only trusted Central report branch scope, cannot widen scope from caller `branch_id`, and suppresses legacy branch low/dead-stock widgets/inventory insights until certified inventory truth is used.
- Backend #86: sales/profit explicit report ranges require paired real `YYYY-MM-DD` dates, deterministic UTC day bounds, correct ordering and at most 366 days; the existing previous-month default remains when no range is supplied.
- Backend #87: branch-scoped inventory reports separate physical stock, sellable non-expired stock, expired stock, provisional/unallocated POS oversell deficit and projected net stock. Real PostgreSQL acceptance seeds distinct branch-A/branch-B non-batch stock, batches, expiry and unallocated sale/return allocations and proves branch isolation without guessing stock authority.
- Backend #88: support/reporting detail limits fail safe into bounded ranges for stock-audit and duplicate-suggestion surfaces instead of allowing malformed or arbitrarily large result sizes.
- Frontend #74: packaged local-POS GST reporting remains Central-owned; Central/offline failures are explicit and retryable and cannot silently become browser/local GST truth.
- Backend #89: `/reports/customers-outstanding` exposes only canonical Central customer financial projection facts, requires `reports:read`, stays tenant-pool-bound and caps each page at 200 rows.
- Backend #90 + Frontend #75: canonical GST reporting consumes immutable sale/return tax snapshots, handles cumulative partial/full reversals exactly, stays branch/range scoped, and exposes total GST without inventing jurisdiction splits.
- Backend #91: sales product performance groups by immutable order-item product identity and prefers sold name/SKU/barcode/source-product snapshots; the PostgreSQL gate proves a later catalog rename/reprice does not rewrite historical sold name or unit price.
- Backend #92 + POSService #260: Central persists immutable category ID/name from the sale event, category reporting consumes that sale-time snapshot, older history without a snapshot stays `Unattributed`, and POS real-SQLite/cross-repository acceptance proves a Central-projected category is frozen at sale creation even if the live catalog is later recategorized.
- Backend #93: existing support/data-quality surfaces are admin-only and tenant-pool-bound; bounded stock-audit/duplicate reads and authenticated actor provenance are explicitly certified. Full tenant backup remains an operational Backup/Recovery concern rather than a Reporting export contract.
- Frontend #76: routed Dashboard Central reachability failure is no longer console-only; it is surfaced accessibly with an explicit retry that remounts the trusted report request surface.
- Historical canonical POS orders whose branch provenance was never recorded are not guessed or backfilled from current device registration because device reassignment can make that inference unsafe; they remain a reporting data-quality/reconciliation concern until exact provenance is available.

## Frozen posture

Reporting/Admin V1 is frozen after release certification except for demonstrated defects. New report formats, historical inference, export formats or browser-side reporting authority require an explicit future product/architecture decision.

## Release decision

**V1 REPORTING / ADMIN RELEASE CERTIFIED.** Every V1 matrix row is now CERTIFIED or explicitly justified N/A, and the merged-main release gate covers POS, Backend, Frontend build/reporting acceptance plus the established real PostgreSQL reporting branch E2E.